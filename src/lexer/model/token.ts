export enum TokenOperator {
    PLUS="+",
    MINUS="-",
    MULT = "*",
    DIVIDE="/",
    OPEN_BRACKET="(",
    CLOSED_BRACKET=")",
    EQUAL = "="
}

export enum TokenType {
    NUMBER= "NUMBER",
    IDENTIFIER="IDENTIFIER"
}

export enum TokenStatement {
    PRINT = "print"
}