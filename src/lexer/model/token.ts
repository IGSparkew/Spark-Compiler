export enum TokenOperator {
    PLUS="+",
    MINUS="-",
    MULT = "*",
    DIVIDE="/",
    OPEN_BRACKET="(",
    CLOSED_BRACKET=")",
    EXCLAMATION="!",
    EQUAL = "=",
    QUOTES= '"',
}

export enum TokenType {
    NUMBER= "NUMBER",
    IDENTIFIER="IDENTIFIER",
    STRING = "STRING"
}

export enum TokenStatement {
    PRINT = "print"
}